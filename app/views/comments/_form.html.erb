
<%= form_for([@post, @post.comments.build]) do |f| %>

	<%= f.label(t("comments.name")) %><br>
	<%= f.text_field :name %><br>
	<br>
	<%= f.label(t("comments.body")) %><br>
	<%= f.text_area :body %><br>
	<br>
	<%= f.submit(t("comments.create_comment")) %>
<% end %>

